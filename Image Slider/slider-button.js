import { LitElement, html, css } from 'https://cdn.jsdelivr.net/gh/lit/dist@3.1.4/all/lit-all.min.js';

class SliderButton extends LitElement {
    static properties = {
        buttonImage: { attribute: true },
        buttonText: { attribute: true }
    };

    static styles = css`
        
    `;
}